name: Keep Alive
on:
    schedule:
        - cron: "*/5 * * * *"
jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - run: curl -fsS --retry 3 https://crm-z2tz.onrender.com/api/health
